import 'package:flutter/material.dart';

class CustomeField extends StatelessWidget {
  const CustomeField({super.key});

  @override
  Widget build(BuildContext context) {
    return TextFormField(
      decoration: InputDecoration(),
    );
  }
}